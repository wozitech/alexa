describe('Alexa Responses', async () => {
    afterEach(() => {
        jest.clearAllMocks();
    });

    it ('alexa responses should be covered by myBuses API unit tests', async () => {
    });
});
